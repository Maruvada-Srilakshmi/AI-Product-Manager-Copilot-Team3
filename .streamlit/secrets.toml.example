# Copy this file to .streamlit/secrets.toml and fill in your key to enable
# real Gemini-powered PRD generation, impact scoring, executive summaries,
# and the chat assistant. Without it, those features fall back to
# deterministic offline templates (the app still works end-to-end).
GEMINI_API_KEY = "your-gemini-api-key-here"
